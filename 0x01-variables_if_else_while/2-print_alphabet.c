#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 *
 * Description: print a to z
*/

int main(void)
{
	char chart = 'a';

	while (chart <= 'z')
	{
		putchar(char);
		chart++;
	}
	putchar('\n');

	return (0);
}
