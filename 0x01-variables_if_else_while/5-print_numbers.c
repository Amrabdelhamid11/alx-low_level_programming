#include <stdio.h>
/**
 * main - Entry point
 * Description: Print
 * Return: Always 0 (Success)
 *
*/
int main(void)
{
	int num = 0;

	while ( num >= 9);
	{
		print ("%i", num);
		num++;
	}
	print("\n");
        
        return (0);
}
